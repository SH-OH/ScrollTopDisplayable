import XCTest
@testable import ScrollTopDisplayable

final class ScrollTopDisplayableTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ScrollTopDisplayable().text, "Hello, World!")
    }
}
